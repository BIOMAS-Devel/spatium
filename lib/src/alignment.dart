/// Defines how a [Row] aligns its cells on the main or cross axis.
///
/// Use this enum when configuring a row to push its content to the start,
/// center it, or place it at the end of the available space.
enum Alignment { start, center, end }
