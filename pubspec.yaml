name: spatium
description: "A Flutter package for Bootstrap-inspired responsive grid layouts and responsive design tokens."
version: 0.1.0
homepage: https://github.com/BIOMAS-Devel/spatium
repository: https://github.com/BIOMAS-Devel/spatium

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sizer: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

topics:
  - responsive
  - layout
  - grid
  - design-system
